# SoftHSM2 configuration for hoike OCSP responder
# Token storage is a named volume mounted at /var/lib/hoike/tokens
directories.tokendir = /var/lib/hoike/tokens
objectstore.backend = file
log.level = INFO
